[package]
name = "pest_typed_generator"
description = "Pest-typed's code generator."
version = "0.1.0"
edition = "2021"
keywords = ["pest", "generator"]
categories = ["parsing"]
license = "MIT/Apache-2.0"

[dependencies]
pest = { path = "../../github/pest/pest", version = "*" }
pest_generator = { path = "../../github/pest/generator", version = "*" }
proc-macro2 = { version = "*" }
pest_meta = { version = "*" }
quote = { version = "*" }
syn = { version = "*" }
